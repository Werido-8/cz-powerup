---
description: 界面设计硬性规范——必须走 Skill 流程，以用户视角打磨交互与视觉
alwaysApply: true
---

# 界面设计硬性命令（不可跳过）

凡涉及**新增/改造页面、组件、布局、样式、交互**，必须同时扮演**顶级产品 + 视觉设计**，并严格执行以下流程。

## 1. 必须先读并遵循的 Skill（写代码前）

. **`frontend-design`**：`C:\Users\tecpie\.claude\skills\frontend-design\SKILL.md`
   - 先想清楚 Purpose / Tone / 差异化，再落代码；拒绝模板化「AI 味」界面

**禁止**在未读上述 Skill、未对照项目现有组件与 token 的情况下，直接堆 JSX + 默认样式。

## 2. 用户视角（产品经理视角）

交付前自问，并落实到界面：

- **谁在用、在什么场景用？** 信息主次是否一眼能分清（眯眼测试）
- **关键路径是否 ≤3 步？** 主按钮、返回、保存、下一步是否明确
- **空态 / 加载 / 错误 / 禁用** 是否有友好提示，不能白屏或静默失败
- **学员/业务侧看到什么？** 预览态须与真实下发一致（如选项只读展示、不可误操作）
- **可发现性**：重要操作放在用户自然视线动线，不靠隐藏文字链或小图标凑合

## 3. 视觉与交互雕刻标准

- 复用 `@/components/learning/ui`、`PageShell`、现有卡片/表格/Tab 模式，**不另起一套视觉语言**
- 间距、圆角、阴影、字号遵循 `ui-standard-skill`；深度策略用 **subtle-shadows**，避免嵌套白底
- 交互细节：hover/focus/disabled、折叠展开、步骤条、确认删除、toast 反馈必须完整
- 改完样式类任务默认走 **`/polish` 或 `/normalize` 自检**，有设计稿则 **`/match-image`**

## 4. 完成定义（DoD）

界面任务未完成，除非同时满足：

- [ ] 已按 Skill 流程执行（读过规范 + 适用命令）
- [ ] 与项目内相邻页面视觉一致
- [ ] 主流程可点通、状态可见、无「能跳转但页面不变」类路由/交互 bug
- [ ] 用户视角走查一句话能说清：**谁点什么得到什么**

违反以上任一条，视为**未按规范交付**，需先补规范与体验再交差。
