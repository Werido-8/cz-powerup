目录可以移动，文件移动
文件置顶后，在列表/卡片模式下不需要显示在最前面；
历史版本，列表可以有icon标记，打开可以查看历史的文件
文件列表模式切换的持久化
我的空间中也可以有快速访问
文件可以置顶

权限是可以配置到角色上，也可以单独指定用户（最终这俩结合）
.文件置顶了，在列表中就不需要排在前面了

管理员可以看到所有状态的文件，普通员工只能看到已审批的文件且已解析的，以及自己的上传的所有文件）

审批后，再出发解析，防止驳回后，浪费解析的资源

